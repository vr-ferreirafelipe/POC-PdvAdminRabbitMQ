version: '3'

services:
  pocpdvadminrabbitmq:
    build: .
    container_name: POC-PdvAdminRabbitMQ
    entrypoint: ./.docker/entrypoint.sh
    volumes:
      - .:/home/node/app
    ports:
      - '9229:9229'
      - '${PORT}:${PORT}'